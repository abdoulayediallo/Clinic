@model IEnumerable<Clinic.Staff_Roles>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Role.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Staff.nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Role.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Staff.nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Staff_Role }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Staff_Role }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Staff_Role })
        </td>
    </tr>
}

</table>
